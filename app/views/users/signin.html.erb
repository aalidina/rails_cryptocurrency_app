<h2>Sign In</h2>

<%= link_to('Log in with Github!', '/auth/gitgub') %>

<%= form_for(:user, :url => {:controller => 'users', :action => 'create'}) do |f| %>
  Username: <%= f.text_field :username %><br>
  Password: <%= f.password_field :password %>
  <br><br>
  <%= f.submit "Sign In" %>
<% end %>
